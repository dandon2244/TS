import Point from "./Point.js";
import Vector from "./Vector.js";

export class Line {
  constructor(point, vector, length) {
    this.point = point;
    this.vector = vector;
    this.length = length;
    // this.endPoint =
  }
}
